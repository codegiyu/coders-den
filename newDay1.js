console.log(7 + 6, "Hello", "World")
console.log(2 + 3) // Addition
console.log(3 - 2) // Subtraction
console.log(2 * 3) // Multiplication
console.log(3 / 2) // Division
console.log(3 % 2) // Modulus - finding remainder
console.log(3 ** 3) // Exponentiation 3 ** 2 == 3 * 3

// PRIMITIVE DATA TYPES
// String, Number, Boolean, undefined, Null, and Symbol.

// Numbers

// Integers - 3, -8, 704, 0
// Float-Point Numbers - -7.5, 0.034

// Strings
"rfyvjhv";
'97rtxxw`0080]';
`gfhjkjkkj`;
'dtr78ihk"'

// Boolean
true;
false;

let giyu;
console.log(giyu);

// type-coercion and type-conversion
let num = "7"
console.log(typeof num)
console.log(num)
num = Number(num)
console.log(typeof num)
console.log(num)

// NON-PRIMITIVE DATA TYPES
// Objects, Arrays, Sets, Maps

// Objects - key(property) and value pair
const myObj = {
    giyu: 7,
    "giyu name": "Hi",
    ["Hello"]: {"World": "Earth"}
}
myObj.name = "Giyu"
console.log(myObj)

// Arrays 
const myArr = ["ray", 9, true, null, undefined, myObj, []]
console.log(myArr)


// Checking Data Types
console.log(typeof 'Asabeneh') // string
console.log(typeof 5) // number
console.log(typeof true) // boolean
console.log(typeof null) // object type
console.log(typeof undefined) // undefined
console.log(typeof giyu) 

let dayOfTheWeek6 = "Saturday";
const PI = 3.142;
const SECONDS_IN_A_MINUTE = 60 * 60;
console.log(dayOfTheWeek6)
dayOfTheWeek6 = "Sunday"
console.log(dayOfTheWeek6)

// A valid JavaScript variable name must follow the following rules:

// A JavaScript variable name should not begin with a number.
// A JavaScript variable name does not allow special characters 
// except dollar sign and underscore.
// A JavaScript variable name follows a camelCase convention.
// A JavaScript variable name should not have space between words.


// 1. Write a single line comment which says, comments can make code readable



// 2. Write another single comment which says, Welcome to 30DaysOfJavaScript



// 3. Write a multiline comment which says, comments can make code readable, easy to reuse and informative